# AURA Phase 1 dependencies (ingest -> DSP features -> quality -> embeddings)
# RAG/Qdrant/FastAPI/Streamlit/experiment-selection deps intentionally
# excluded until their respective phases, per scope.

torch>=2.2,<3.0
torchaudio>=2.2,<3.0
transformers>=4.40,<5.0

numpy>=1.26,<2.0
scipy>=1.11
pandas>=2.2
pyarrow>=15.0

librosa>=0.10
soundfile>=0.12

diskcache>=5.6
PyYAML>=6.0

pytest>=8.0
